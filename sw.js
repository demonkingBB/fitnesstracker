// Service Worker placeholder to allow mobile app installation
self.addEventListener('fetch', function(event) {
  // Keeps the app installable without caching assets aggressively during your dev testing
});